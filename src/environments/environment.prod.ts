// Currently, the development(environment.ts and environment.dev.ts) and production environments are the same.
// In case any of this changes, these files need to be adjusted accordingly.

export const environment = {
  production: true,
  api_base_url: `https://backend:3000`,
  api_route_tasks: 'tasks',
};
